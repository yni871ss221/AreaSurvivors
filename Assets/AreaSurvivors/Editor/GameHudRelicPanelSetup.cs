namespace AreaSurvivors.Editor
{
    // HUD relic panel layout setup was intentionally removed.
    // Existing HUD layout must stay authored in the Scene, not rebuilt from Editor code.
    public static class GameHudRelicPanelSetup
    {
    }
}
